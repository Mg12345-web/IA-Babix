# Exemplo de variáveis de ambiente (não subir esse arquivo com valores reais)

OPENAI_API_KEY= # definida apenas no Railway
OPENAI_MODEL=gpt-4o-mini
CHROMA_DIR=./dados/chroma
EMBEDDING_MODEL=all-MiniLM-L6-v2
